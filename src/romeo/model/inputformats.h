#ifndef INPUTFORMATS_H
#define INPUTFORMATS_H

#endif // INPUTFORMATS_H

namespace romeo{
namespace model{

enum InputFormat2D { BMP, PNG, JPEG, TIFF };
enum InputFormat2DT{ AVI, MPEG, MJPEG };
enum InputFormat3D{ NIFTY, ANALYZE };
}
}
